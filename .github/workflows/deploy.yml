name: Railway Auto Deploy

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment triggered
        run: echo "Push to main detected. Railway will automatically build and deploy."
